describe("Checklist Suite", function() {
	var contextRoot, checklist;
	beforeEach(function() {
	    contextRoot = "app";
		//checklist = Checklist(contextRoot);
	});

	it("save user should make an ajax call", function() {
		expect(true).toBe(true);
	});
});